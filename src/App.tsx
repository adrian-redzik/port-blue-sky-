import React from 'react';
import { PaymentDates } from './components/PaymentDates';
import { Box } from '@mui/material';

function App() {
    return (
        <Box p={3}>
            <PaymentDates/>
        </Box>
    );
}

export default App;
